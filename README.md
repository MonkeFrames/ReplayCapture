# ReplayCapture
ReplayCapture is a lightweight MonkeFrames replay recorder which starts and ends automatically when changing rooms so you can save replays while not running MonkeFrames.

It automatically saves replays to the MonkeFrames `replays` folder under the naming scheme `MFAutoReplay [room code] [date/time].mfreplay`.